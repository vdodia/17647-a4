apiVersion: v1
kind: Service
metadata:
  name: book-command-service
  namespace: bookstore-ns
spec:
  type: ClusterIP
  selector:
    app: book-command-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
