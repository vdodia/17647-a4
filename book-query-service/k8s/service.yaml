apiVersion: v1
kind: Service
metadata:
  name: book-query-service
  namespace: bookstore-ns
spec:
  type: ClusterIP
  selector:
    app: book-query-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
